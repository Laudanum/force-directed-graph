<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
    {{>styles}}
  </head>

  <body class="record {{record.category.nicename}}">
    <header>{{>header}}</header>
    <main id="container">{{>main}}</main>
  </body>
  {{>scripts}}
</html>
