<!doctype html>
<html>
  <head>
    {{>head}}
    {{>styles}}
  </head>
  <body>
    <div>
      <header>{{>header}}</header>
      <main>{{>main}}</main>
      <footer>{{>footer}}</footer>
    </div>
  </body>
  {{>scripts}}
</html>
